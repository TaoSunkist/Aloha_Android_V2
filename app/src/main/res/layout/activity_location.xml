<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.wealoha.social.LocationActivity" >

    <RelativeLayout
        android:id="@+id/location_menu_bar"
        style="@style/aloha_title_container" >

        <ImageView
            android:id="@+id/location_back"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_alignParentLeft="true"
            android:layout_alignParentStart="true"
            android:clickable="true"
            android:contentDescription="@null"
            android:scaleType="center"
            android:src="@drawable/nav_back" />

        <TextView
            android:id="@+id/feed_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:layout_centerVertical="true"
            android:text="@string/choose_location"
            android:textColor="@color/black_text"
            android:textSize="12sp" />
    </RelativeLayout>

    <View
        android:id="@+id/location_line"
        android:layout_width="match_parent"
        android:layout_height="1px"
        android:layout_below="@id/location_menu_bar"
        android:background="@color/nav_line" />

    <ListView
        android:id="@+id/location_country_listview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/location_line"
        android:background="@color/background"
        android:dividerHeight="-1px" />

</RelativeLayout>