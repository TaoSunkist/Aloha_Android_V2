<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical" >

    <RelativeLayout
        android:id="@+id/title_bar"
        style="@style/aloha_title_container"
        android:background="@drawable/layout_silde" >

        <ImageView
            android:id="@+id/pro_feature_back_tv"
            style="@style/aloha_title_back_btn" />

        <TextView
            android:id="@+id/title_tv"
            style="@style/aloha_title"
            android:text="@string/advanced_features" />
    </RelativeLayout>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content" >

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" >

            <androidx.cardview.widget.CardView
                android:id="@+id/content_cardview"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_margin="18dp"
                card_view:cardElevation="2dp" >

                <LinearLayout
                    android:id="@+id/cardview_root"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:background="@color/white_color"
                    android:orientation="vertical" />
            </androidx.cardview.widget.CardView>

            <TextView
                android:id="@+id/rule_of_profeature"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:text="@string/invite_a_friend_to_get_the_invitation_code"
                android:textColor="@color/gray_text"
                android:textSize="12sp" />
        </LinearLayout>
    </ScrollView>

</LinearLayout>